apiVersion: v1
kind: Service
metadata:
  name: {{ include "apt.fullname" . }}
  labels:
    {{- include "apt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apt.selectorLabels" . | nindent 4 }}
