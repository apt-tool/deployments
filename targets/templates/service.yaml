apiVersion: v1
kind: Service
metadata:
  name: {{ include "targets.fullname" . }}
  labels:
    {{- include "targets.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "targets.selectorLabels" . | nindent 4 }}
